"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";

export function HikeQuickActions({id,past,published,cancelled}:{id:string;past:boolean;published:boolean;cancelled:boolean}){const router=useRouter();const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");async function action(value:"DUPLICATE"|"PUBLISH"|"UNPUBLISH"|"CANCEL"){const prompt=value==="DUPLICATE"?"¿Crear una copia en borrador?":value==="CANCEL"?"¿Cancelar este hike? Las reservaciones no se modificarán automáticamente.":value==="UNPUBLISH"?"¿Despublicar este hike?":"¿Publicar este hike?";if(!window.confirm(prompt))return;setBusy(true);setMessage("");const response=await fetch(`/api/admin/hikes/${id}/actions`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:value})});const result=await response.json() as {id?:string;error?:string};setBusy(false);if(!response.ok)return setMessage(result.error??"No pudimos completar la acción.");if(result.id){router.push(`/admin/hikes/${result.id}/editar`);return;}router.refresh();}
  return <div className="hike-card-actions"><Link className="button button-primary" href={`/admin/hikes/${id}`}>{past?"VER RESULTADOS":"ADMINISTRAR"}</Link><details><summary>OTRAS ACCIONES</summary><div><Link href={`/admin/hikes/${id}/editar`}>EDITAR</Link><Link href={`/admin/hikes/${id}?tab=inscripciones`}>PARTICIPANTES</Link><Link href={`/admin/hike-mode?hike=${id}`}>MODO HIKE</Link><Link href={`/admin/hikes/${id}?tab=fotos`}>FOTOS</Link><Link href={`/admin/hikes/${id}?tab=pagos`}>FINANZAS</Link><button disabled={busy} onClick={()=>void action("DUPLICATE")}>DUPLICAR</button>{!cancelled&&<button disabled={busy} onClick={()=>void action(published?"UNPUBLISH":"PUBLISH")}>{published?"DESPUBLICAR":"PUBLICAR"}</button>}{!past&&!cancelled&&<button className="danger" disabled={busy} onClick={()=>void action("CANCEL")}>CANCELAR</button>}</div></details>{message&&<small role="alert">{message}</small>}</div>;
}
